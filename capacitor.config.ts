import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'openotd-client',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
